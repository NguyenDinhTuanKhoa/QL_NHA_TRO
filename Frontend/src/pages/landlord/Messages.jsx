import React from 'react';
import MessagesComponent from '../user/Messages';

// Sử dụng lại component Messages từ user
const LandlordMessages = () => {
    return <MessagesComponent />;
};

export default LandlordMessages;
